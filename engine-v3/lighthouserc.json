{
  "//": "Lighthouse CI config (performance gate). Builds the SPA, serves dist via vite preview, and asserts category scores + a few key audits. Run with: npm run test:lhci (needs @lhci/cli + Chrome). CI runs it in the perf job.",
  "ci": {
    "collect": {
      "startServerCommand": "npm run web:build && npm --prefix gui run preview -- --port 4178 --strictPort",
      "url": ["http://localhost:4178/"],
      "numberOfRuns": 1,
      "settings": { "preset": "desktop" }
    },
    "assert": {
      "assertions": {
        "categories:performance": ["error", { "minScore": 0.85 }],
        "categories:accessibility": ["error", { "minScore": 0.9 }],
        "categories:best-practices": ["warn", { "minScore": 0.9 }],
        "total-byte-weight": ["warn", { "maxNumericValue": 1500000 }],
        "unused-javascript": "off"
      }
    },
    "upload": { "target": "temporary-public-storage" }
  }
}
